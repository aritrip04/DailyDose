#include <stdio.h>
#include <string.h>
struct STUDENT{
    int roll;
    char name[10];
    float marks;
};
int main(){
    struct STUDENT s;
    s.roll = 22;
    strcpy(s.name, "Aritri");
    s.marks = 67;
    printf("Roll: %d\n", s.roll);
    printf("Name: %s\n", s.name);
    printf("Marks: %.2f\n", s.marks);
    return 0;
}
